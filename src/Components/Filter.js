import React from 'react'

const Filter = () => {
  return(
    <div>
      <select className="select">
        <option>Filter by Region</option>
      </select>
    </div>
  )
}

export default Filter
